---
"irsdk-node": minor
---

Updates all wrapped broadcast API messages to follow the correct API signature for their given messages. Also added `broadcastUnsafe` function, allowing to call the full broadcast API without guardrails.
